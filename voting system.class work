include <stdio.h>
void main()
{
    int age;
    printf("enter the age of the person");
    scanf("%d",&age);
    if(age>=18)
    {
        printf("eligible for voting");
    }
    else 
    {
        printf("not eligible for voting");
    }
    getchar();
}
